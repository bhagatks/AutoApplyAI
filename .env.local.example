# Copy this file to `.env.local` for onboarding UI testing only.
# `.env.local` is gitignored — never commit it.
#
# REMOVE when onboarding testing is done:
#   1. Delete `.env.local`
#   2. Set VITE_ONBOARDING_DEV_INJECT=false or remove the file
#   3. Rebuild the extension

VITE_ONBOARDING_DEV_INJECT=true
VITE_DEV_KEY_GEMINI=
VITE_DEV_KEY_OPENAI=
VITE_DEV_KEY_ANTHROPIC=
VITE_DEV_KEY_GROK=
